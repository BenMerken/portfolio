import './styles.scss';

function Contact() {
  return (
    <section id="contact" className="contact">
      <h2>Get in touch!</h2>
      <p>Contrary to popular belief, I am rather approachable!</p>
      {/* TODO: See if I can use a form instead of a mailto link for contact. Otherwise, better to link to LinkedIn, I guess. */}
    </section>
  );
}

export default Contact;
